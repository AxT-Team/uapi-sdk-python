# coding: utf-8

"""
    UAPI OpenAPI

    Generated from UAPI JSON. Code is the interactive documentation: rich descriptions, examples, and x-extensions preserved.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class PostTextAesEncryptAdvancedRequest(BaseModel):
    """
    PostTextAesEncryptAdvancedRequest
    """ # noqa: E501
    iv: Optional[StrictStr] = Field(default=None, description="自定义IV（可选，Base64编码，16字节）。GCM模式无需此参数")
    key: StrictStr = Field(description="加密密钥（支持任意长度）")
    mode: Optional[StrictStr] = Field(default=None, description="加密模式：GCM/CBC/ECB/CTR/OFB/CFB（可选，默认GCM）")
    output_format: Optional[StrictStr] = Field(default=None, description="输出格式：base64（默认）或hex")
    padding: Optional[StrictStr] = Field(default=None, description="填充方式：PKCS7/ZERO/NONE（可选，默认PKCS7）")
    text: StrictStr = Field(description="待加密的明文文本")
    __properties: ClassVar[List[str]] = ["iv", "key", "mode", "output_format", "padding", "text"]

    @field_validator('mode')
    def mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['GCM', 'CBC', 'ECB', 'CTR', 'OFB', 'CFB']):
            raise ValueError("must be one of enum values ('GCM', 'CBC', 'ECB', 'CTR', 'OFB', 'CFB')")
        return value

    @field_validator('output_format')
    def output_format_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['base64', 'hex']):
            raise ValueError("must be one of enum values ('base64', 'hex')")
        return value

    @field_validator('padding')
    def padding_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['PKCS7', 'ZERO', 'NONE']):
            raise ValueError("must be one of enum values ('PKCS7', 'ZERO', 'NONE')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PostTextAesEncryptAdvancedRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PostTextAesEncryptAdvancedRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "iv": obj.get("iv"),
            "key": obj.get("key"),
            "mode": obj.get("mode"),
            "output_format": obj.get("output_format"),
            "padding": obj.get("padding"),
            "text": obj.get("text")
        })
        return _obj


