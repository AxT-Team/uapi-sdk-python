# coding: utf-8

"""
    UAPI

    UAPI 官方接口文档

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class PostTranslateStreamRequest(BaseModel):
    """
    PostTranslateStreamRequest
    """ # noqa: E501
    query: StrictStr = Field(description="待翻译的文本内容")
    to_lang: StrictStr = Field(description="目标语言，支持：中文、英文")
    from_lang: Optional[StrictStr] = Field(default='auto', description="源语言，支持：中文、英文、auto（自动检测）。默认为auto")
    tone: Optional[StrictStr] = Field(default=None, description="语气参数，可选")
    __properties: ClassVar[List[str]] = ["query", "to_lang", "from_lang", "tone"]

    @field_validator('to_lang')
    def to_lang_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['中文', '英文']):
            raise ValueError("must be one of enum values ('中文', '英文')")
        return value

    @field_validator('from_lang')
    def from_lang_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['中文', '英文', 'auto']):
            raise ValueError("must be one of enum values ('中文', '英文', 'auto')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PostTranslateStreamRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PostTranslateStreamRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "query": obj.get("query"),
            "to_lang": obj.get("to_lang"),
            "from_lang": obj.get("from_lang") if obj.get("from_lang") is not None else 'auto',
            "tone": obj.get("tone")
        })
        return _obj


