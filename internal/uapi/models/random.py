# coding: utf-8

"""
    UAPI OpenAPI

    Generated from UAPI JSON. Code is the interactive documentation: rich descriptions, examples, and x-extensions preserved.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uapi.models.random_authorinfo import RandomAuthorinfo
from typing import Optional, Set
from typing_extensions import Self

class Random(BaseModel):
    """
    Random
    """ # noqa: E501
    author: Optional[StrictStr] = Field(default=None, description="作者或说话者。")
    authorinfo: Optional[RandomAuthorinfo] = None
    category: Optional[StrictStr] = Field(default=None, description="语录分类。")
    content: Optional[StrictStr] = Field(default=None, description="语录正文。")
    content_length: Optional[StrictInt] = Field(default=None, description="正文字数。", alias="contentLength")
    corpus: Optional[StrictStr] = Field(default=None, description="所属语料库标识。")
    created_at: Optional[StrictStr] = Field(default=None, description="语料入库时间戳（秒），部分语料返回。", alias="createdAt")
    matched_tags: Optional[List[StrictStr]] = Field(default=None, description="命中的标签，部分模式返回。", alias="matchedTags")
    source: Optional[StrictStr] = Field(default=None, description="语录出处或来源。")
    uuid: Optional[StrictStr] = Field(default=None, description="语录唯一标识。")
    __properties: ClassVar[List[str]] = ["author", "authorinfo", "category", "content", "contentLength", "corpus", "createdAt", "matchedTags", "source", "uuid"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Random from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of authorinfo
        if self.authorinfo:
            _dict['authorinfo'] = self.authorinfo.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Random from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "author": obj.get("author"),
            "authorinfo": RandomAuthorinfo.from_dict(obj["authorinfo"]) if obj.get("authorinfo") is not None else None,
            "category": obj.get("category"),
            "content": obj.get("content"),
            "contentLength": obj.get("contentLength"),
            "corpus": obj.get("corpus"),
            "createdAt": obj.get("createdAt"),
            "matchedTags": obj.get("matchedTags"),
            "source": obj.get("source"),
            "uuid": obj.get("uuid")
        })
        return _obj


